export interface PocResponse {
    id: number
    centerId: number
    name: string
    phoneNumber: string
    fullAddress: string
    CollectionDay: string
    CollectionBy: string
    CollectionFrequency: string

  }