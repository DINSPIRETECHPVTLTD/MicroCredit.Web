# Copy to .env.development and set your dev API URL.
# Used when building with: npm run build -- --mode development
# Or set VITE_API_URL in your CI/CD before running the build for dev deploy.
VITE_API_URL=https://mcs.api.dinspire.in/