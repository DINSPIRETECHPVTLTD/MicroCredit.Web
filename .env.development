# Development – loaded when running npm run dev or build:dev
# Must point at the LOCAL API with shrink-base + payment-child partials.
# Remote https://mcs.api.dinspire.in still runs the OLD same-row ApplyPartial
# path until that host is redeployed with the redesigned RecoveryPostingService.
# Local Kestrel (dotnet run --launch-profile https): https://localhost:7119
# Local IIS Express: https://localhost:44378
VITE_API_URL=https://localhost:44378/
